# 多轨音乐工作台：复现计划

## 目标与分工

复现灵谱的核心工作流：音频上传 → 音源分离 → 逐轨音符转录 → 可编辑工程 → MIDI 导出。采用自己的代码与开源模型；不调用灵谱私有服务，不假定使用与灵谱相同的模型。

你使用 Figma 生成前端；我先完成后端、模型适配、接口文档与前端 SDK。视觉稿完成后再接入真实编辑、同步试听和导出。当前是本机单用户开发版本，公网多用户发布属于后续阶段。

## 框架

| 部分 | 选择 | 原因 |
| --- | --- | --- |
| 前端 | React + TypeScript + Vite | 便于接收 Figma 生成代码；音乐编辑器主要在浏览器运行 |
| 样式与组件 | Tailwind CSS + shadcn/ui | 页面布局与组件便于复用 |
| 状态 | Zustand（编辑与播放状态）、TanStack Query（服务端数据） | 将高频编辑与异步任务分开 |
| 波形/试听/音符 | WaveSurfer.js / Tone.js / Canvas | 波形显示、统一音频时钟、可缩放音符网格 |
| 后端 | Python + FastAPI + Pydantic | 音频模型生态与自动生成 OpenAPI |
| 任务进程 | 独立 Python worker，SQLite 持久队列 | 本地起步无需 Redis；API 进程不跑模型 |
| 数据/文件 | SQLite + 本地文件目录 | 可恢复项目、任务和进度；未来迁移 PostgreSQL + 对象存储 |
| 分离 | Demucs 4 轨基线，可选实验 6 轨 | 先建立可测基线；以后通过适配器换 RoFormer 等模型 |
| 转录 | Basic Pitch | 先覆盖有音高轨道；鼓保留音频并明确标注尚未转录 |
| 导出 | mido，标准多轨 MIDI | 保留音高、起止时间、力度、音色与鼓通道 |

## 实施顺序与验收

### P0：合同与前端设计（先做）
- 固定三页：项目列表 `/projects`、上传 `/upload`、编辑器 `/projects/:id`。
- 固定 Project、Track、Note、Job 的字段，音符时间统一用秒。
- 提供 Figma 提示词、TypeScript SDK、明确标记为演示的数据。
- 验收：Figma 不依赖模型也能完成全部页面与状态；后端 OpenAPI 与 SDK 对齐。

### P1：可运行后端基础（本轮开始实现）
- 音频上传、格式和大小校验、项目与任务持久化。
- 独立 worker 领取任务；阶段事件通过 SSE 推送，可断线重连。
- 项目读取、音符与混音参数保存、版本冲突检测。
- 原音频与分轨音频下载、标准 MIDI 导出。
- 验收：上传→领取任务→保存工程→编辑→导出链路通过集成测试；失败和取消有明确终态。

### P2：接通真实模型（与前端设计并行）
- 模型依赖与普通 API 环境隔离，模型/缓存全部放在项目指定目录。
- 先以单乐器音频验证 Basic Pitch，再验证 Demucs + Basic Pitch。
- 保留分离音频，逐轨返回识别结果；鼓与不支持项显式给出提示，不伪造音符。
- 短合成音频用于验证模型能运行，真实歌曲用于后续质量对比。
- 验收：真实推理产生非空有效 MIDI；记录设备、时长、耗时、模型版本与限制。程序跑通不等于达到原站音质。

### P3：接入你的 Figma 前端
- 接入真实上传、任务进度、项目列表和错误重试。
- Canvas 钢琴卷帘：增删音符、拖动音高和位置、拖动时长、框选、缩放、撤销重做。
- 音轨静音/独奏/音量/声像；原音频与 MIDI 分模式试听，共用统一播放时钟。
- 保存与导出；发生版本冲突时保留本地编辑并提示重新加载或合并。
- 验收：音符修改后试听与导出一致，刷新后仍在；无假进度、无把演示数据当真实转录。

### P4：识别效果提升
- 收集具有已知 MIDI/分轨真值的测试样本，按乐器统计错音、漏音、起止误差。
- 单独加入鼓转录、钢琴专用转录、弦乐分离/转录适配器。
- 节拍检测、变速图、节奏量化；量化可撤销，不覆盖原始转录。
- 对比分离模型与转录模型组合，而不是仅凭试听挑选。
- 验收：在固定样本集上优于 P2 基线，列出失败案例。

### P5：部署与多人使用
- 登录与项目归属、文件访问控制、配额、并发限制、日志与任务恢复。
- PostgreSQL + Redis/任务队列 + 对象存储 + 独立 GPU worker。
- 前端与 API 可分别部署，GPU 推理服务独立部署。
- 验收：跨账号隔离、任务重启恢复、资源和费用上限明确。当前版本不直接公开为多人服务。

## 本轮边界

先交付详细计划、Figma 前端说明、SDK 与可测试的本机后端。前端由你生成，收到代码后进入 P3。模型运行验证会单独报告“实际验证”与“尚未验证”，不将占位实现作为识别完成。

## 时间与数据约定

- 所有音符使用 `startSeconds`、`durationSeconds`；音高和力度为 MIDI 整数。
- `bpm` 初版为用户给定值或明确提示的默认值，不能当作自动检测结果。
- 改 BPM 默认仅改变节拍网格与 MIDI 的时间映射，保持音符秒数不变；整曲变速是独立功能。
- MIDI 导出采用 480 ticks/quarter，并把秒数映射到当前 BPM；鼓使用专用通道。
- 所有演示工程展示“演示数据”，不描述为真实分轨结果。

## 官方依据

- FastAPI: https://fastapi.tiangolo.com/
- Demucs: https://github.com/facebookresearch/demucs
- Basic Pitch: https://github.com/spotify/basic-pitch
- WaveSurfer: https://wavesurfer.xyz/docs/
- Tone.js: https://tonejs.github.io/
